# 阶段任务（Stage 2）— 开启聚合并验证示例贡献者

## 阶段目标
- 在灰度环境开启聚合开关，接入一个“示例贡献者”（最小实现）验证同乘区数学正确性与日志开关。

## 具体任务
- 提供一个最小 `OnHitModifier` 示例（仅在测试或样例模块内，不涉及器官迁移）。
- 补充聚合单元测试与简单基准（可选）。
- 开启 `UNIFY_ONHIT_BUCKETS` 进行本地/灰度验证。

## 依赖关系
- 依赖：Stage 1 已完成。

## 验收标准
- 聚合测试覆盖通过；示例贡献者生效；无重复增伤。

