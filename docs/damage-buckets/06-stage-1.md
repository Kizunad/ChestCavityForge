# 阶段任务（Stage 1）— 引入聚合骨架（默认关闭）

## 阶段目标
- 在不影响现有行为的前提下，落地聚合上下文、接口与接线；开关默认关闭。

## 具体任务
- 定义 `AggregationContext` 与 `OnHitModifier` 接口（仅声明，不迁移器官）。
- 在 `ChestCavityUtil.onHit` 接入聚合阶段与一次性应用，受 `UNIFY_ONHIT_BUCKETS` 控制。
- 实现 `LegacyBridge` 跳重机制（标记/识别已迁移）。
- 新增最小调试输出（DEBUG 开关）。

## 依赖关系
- 依赖：无（仅引用现有事件接入点）。

## 验收标准
- 开关关闭：功能零差异；单元测试通过。
- 开关开启：不报错、聚合阶段可运行（即便此时无贡献者）。

