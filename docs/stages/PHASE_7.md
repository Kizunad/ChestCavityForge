# Phase 7｜最终清理与发布

## 阶段目标
- 删除未引用代码；完成评审与发布候选版本。

## 任务列表
- 删除高级轨迹/模板/意图中未引用实现；精简枚举。
- 删除旧 Goal 与辅助类；移除残留开关路径中的死代码。
- 代码审查：无未使用 import/无 TODO 杂项/公开 API 注释齐全。

## 依赖关系
- 依赖前序全部阶段完成与回归通过。

## 验收标准
- `./gradlew build` 与 `./gradlew test` 通过；手动全功能回归稳定。

## 风险与回退
- 低中；保留删除列表，必要时撤销最后一步提交。

