# Phase 5｜客户端与网络

## 阶段目标
- 渲染降噪；TUI/Gecko/覆盖为可选；网络仅必要同步。

## 任务列表
- `FlyingSwordRenderer` 保留默认路径；Gecko/覆盖/视觉档 behind `ENABLE_GEO_OVERRIDE_PROFILE`。
- 关闭 TUI 时的文本提示降级；保留最小命令反馈。
- 核查网络载荷与频率，避免刷屏与风暴。

## 依赖关系
- 依赖 Phase 1 开关与既有渲染路径。

## 验收标准
- 默认关闭高级项时渲染与交互正常；无日志/网络刷屏。

## 风险与回退
- 低中；可通过开关重新启用高级项。

