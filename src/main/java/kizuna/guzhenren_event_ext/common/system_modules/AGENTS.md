# Package: common.system_modules

This package contains the concrete implementations of the modular components defined by the registries. It is organized into sub-packages for `triggers`, `conditions`, and `actions`. This is where the actual logic for what to check and what to do resides.
