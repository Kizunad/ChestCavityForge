# Package: common.event.api

This package contains the public API for our custom events. Other modules or addons can listen for these events to integrate with the event extension system. It defines the contract of the events, such as `GuzhenrenStatChangeEvent`.
