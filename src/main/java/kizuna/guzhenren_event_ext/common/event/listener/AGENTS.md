# Package: common.event.listener

This package is intended for classes that listen to standard Forge or Minecraft events (e.g., `PlayerTickEvent`, `LivingDeathEvent`). These listeners can act as sources for new types of custom events in the future.
