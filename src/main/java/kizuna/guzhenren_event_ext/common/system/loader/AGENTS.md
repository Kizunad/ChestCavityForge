# Package: common.system.loader

This package will contain classes responsible for loading and parsing data from files. Its primary component will be the `EventLoader`, which reads and validates the event definitions from the JSON files.
