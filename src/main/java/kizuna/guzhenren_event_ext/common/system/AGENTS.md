# Package: common.system

This package contains the core logic and managers of the event system. This includes the `PlayerStatWatcher` (which detects changes) and the `EventManager` (which will process the events).
