# Package: common.system.registry

This package will hold the registries for the system's modular components. This includes the `TriggerRegistry`, `ConditionRegistry`, and `ActionRegistry`, which allow for the dynamic registration of new event-processing logic.
