# Package: common.config

This package holds configuration-related classes, such as the `Gamerules` for enabling/disabling the module and any future `ModConfig` files for fine-tuning settings like the polling interval.
