# 阶段2: 物品与交互 - 详细任务清单

## 📋 阶段概览

- **目标**: 实现多重剑影蛊物品及其召唤/召回交互逻辑
- **预计工作量**: 3天
- **依赖**: 阶段1 (核心实体) ✅ 已完成
- **输出**: 可用的多重剑影蛊物品,支持召唤/召回分身

---

## 📝 任务清单

### 2.1 创建 `DuochongjianyingGuItem.java` (核心物品类)

**文件位置**: `src/main/java/net/tigereye/chestcavity/compat/guzhenren/item/jian_dao/DuochongjianyingGuItem.java`

#### ✅ 2.1.1 创建基础类结构
- [ ] 创建类,继承 `GuChongItem` 或 `Item`
- [ ] 添加构造函数,设置物品属性:
  ```java
  public DuochongjianyingGuItem() {
      super(new Properties()
          .stacksTo(1)  // 不可堆叠
          .rarity(Rarity.RARE));
  }
  ```

#### ✅ 2.1.2 实现 `use()` 方法 (核心交互)
**优先级**: P0 (必须)

**实现要点**:
1. **Shift优先处理** (修正v1.0交互不一致问题):
   ```java
   @Override
   public InteractionResultHolder<ItemStack> use(Level level, Player player, InteractionHand hand) {
       ItemStack stack = player.getItemInHand(hand);

       // 1. 所有权校验
       if (!isOwnedBy(stack, player)) {
           player.displayClientMessage(Component.literal("§c这不是你的蛊虫!"), true);
           return InteractionResultHolder.fail(stack);
       }

       // 2. 优先处理 Shift + 右键 (打开界面)
       if (player.isShiftKeyDown()) {
           return handleOpenInventory(level, player, stack);
       }

       // 3. 处理召唤/召回
       return handleSummonOrRecall(level, player, stack);
   }
   ```

2. **边界检查**:
   - ✅ 仅服务器端执行逻辑 (`if (level.isClientSide)`)
   - ✅ 所有权校验
   - ✅ 防止快速重复点击 (可选: 添加冷却)

#### ✅ 2.1.3 实现 `handleOpenInventory()` 方法
**优先级**: P0 (必须)

**实现步骤**:
```java
private InteractionResultHolder<ItemStack> handleOpenInventory(
    Level level,
    Player player,
    ItemStack stack
) {
    if (level.isClientSide) {
        return InteractionResultHolder.success(stack);
    }

    // 1. 检查分身是否存在
    PersistentGuCultivatorClone clone = findClone(level, stack);

    if (clone == null) {
        player.displayClientMessage(
            Component.literal("§c分身未召唤或已死亡!"),
            true
        );
        return InteractionResultHolder.fail(stack);
    }

    // 2. 检查距离 (可选: 最大10格)
    if (player.distanceToSqr(clone) > 100.0) {
        player.displayClientMessage(
            Component.literal("§c分身距离太远!"),
            true
        );
        return InteractionResultHolder.fail(stack);
    }

    // 3. 打开界面
    clone.openInventoryMenu((ServerPlayer) player);

    return InteractionResultHolder.success(stack);
}
```

**注意事项**:
- 阶段2暂时调用 `clone.openInventoryMenu()`,实际GUI在阶段3实现
- 可以先用占位提示: `player.displayClientMessage(Component.literal("§e[阶段3] 打开分身界面"), true);`

#### ✅ 2.1.4 实现 `handleSummonOrRecall()` 方法
**优先级**: P0 (必须)

**实现逻辑**:
```java
private InteractionResultHolder<ItemStack> handleSummonOrRecall(
    Level level,
    Player player,
    ItemStack stack
) {
    if (level.isClientSide) {
        return InteractionResultHolder.success(stack);
    }

    // 1. 检查是否已有分身存在
    PersistentGuCultivatorClone existingClone = findClone(level, stack);

    if (existingClone != null) {
        // 存在 -> 召回
        recallClone(stack, existingClone, player);
        return InteractionResultHolder.success(stack);
    } else {
        // 不存在 -> 召唤
        PersistentGuCultivatorClone newClone = summonClone(level, player, stack);

        if (newClone != null) {
            // 保存UUID到物品NBT
            stack.getOrCreateTag().putUUID("CloneUUID", newClone.getUUID());
            stack.getTag().putString("DimensionKey", level.dimension().location().toString());

            player.displayClientMessage(
                Component.literal("§6分身已召唤"),
                true
            );
            return InteractionResultHolder.success(stack);
        } else {
            player.displayClientMessage(
                Component.literal("§c召唤失败!"),
                true
            );
            return InteractionResultHolder.fail(stack);
        }
    }
}
```

#### ✅ 2.1.5 实现 `findClone()` 辅助方法
**优先级**: P0 (必须)

**实现要点**:
```java
@Nullable
private PersistentGuCultivatorClone findClone(Level level, ItemStack stack) {
    if (!stack.hasTag() || !stack.getTag().hasUUID("CloneUUID")) {
        return null;
    }

    UUID cloneUUID = stack.getTag().getUUID("CloneUUID");

    // 检查维度一致性
    if (stack.getTag().contains("DimensionKey")) {
        String savedDim = stack.getTag().getString("DimensionKey");
        String currentDim = level.dimension().location().toString();

        if (!savedDim.equals(currentDim)) {
            // 跨维度场景: 惰性清理 (不立即清理,等待用户下次操作)
            return null;
        }
    }

    // 查找实体
    Entity entity = ((ServerLevel) level).getEntity(cloneUUID);

    if (entity instanceof PersistentGuCultivatorClone clone && clone.isAlive()) {
        return clone;
    }

    // 实体已死亡或不存在: 惰性清理
    return null;
}
```

#### ✅ 2.1.6 实现 `summonClone()` 方法
**优先级**: P0 (必须)

**实现步骤**:
```java
@Nullable
private PersistentGuCultivatorClone summonClone(
    Level level,
    Player player,
    ItemStack stack
) {
    // 1. 计算生成位置 (玩家前方2格)
    Vec3 spawnPos = player.position().add(player.getLookAngle().scale(2.0));

    // 2. 创建新分身 (使用静态工厂方法)
    PersistentGuCultivatorClone clone = PersistentGuCultivatorClone.spawn(
        (ServerLevel) level,
        player,
        spawnPos
    );

    if (clone == null) {
        return null;
    }

    // 3. 如果物品NBT中有存储的数据,恢复它
    if (stack.hasTag() && stack.getTag().contains("CloneData")) {
        CompoundTag cloneData = stack.getTag().getCompound("CloneData");
        clone.deserializeFromItemNBT(cloneData);
    }

    // 4. 播放召唤特效
    level.playSound(
        null,
        clone.blockPosition(),
        SoundEvents.ENDERMAN_TELEPORT,
        SoundSource.PLAYERS,
        1.0f,
        0.8f
    );

    return clone;
}
```

#### ✅ 2.1.7 实现 `recallClone()` 方法
**优先级**: P0 (必须)

**实现步骤**:
```java
private void recallClone(
    ItemStack stack,
    PersistentGuCultivatorClone clone,
    Player player
) {
    // 1. 保存分身数据到物品NBT (使用专用方法)
    CompoundTag cloneData = clone.serializeToItemNBT();
    stack.getOrCreateTag().put("CloneData", cloneData);

    // 2. 播放召回特效
    clone.level().playSound(
        null,
        clone.blockPosition(),
        SoundEvents.ENDERMAN_TELEPORT,
        SoundSource.PLAYERS,
        1.0f,
        1.2f
    );

    // 3. 移除分身实体
    clone.discard();

    // 4. 清除UUID (因为实体已不存在)
    stack.getTag().remove("CloneUUID");
    stack.getTag().remove("DimensionKey");

    // 5. 提示玩家
    player.displayClientMessage(
        Component.literal("§6分身已召回"),
        true
    );
}
```

#### ✅ 2.1.8 实现 `cleanupCloneData()` 方法 (惰性清理)
**优先级**: P1 (建议)

**用途**: 当检测到分身已死亡/跨维度时,清理悬挂的UUID

```java
private void cleanupCloneData(ItemStack stack) {
    if (stack.hasTag()) {
        stack.getTag().remove("CloneUUID");
        stack.getTag().remove("CloneData");
        stack.getTag().remove("DimensionKey");
    }
}
```

**调用时机**:
- `findClone()` 返回 null 时
- 玩家再次尝试召唤时

#### ✅ 2.1.9 实现 `isOwnedBy()` 方法 (所有权校验)
**优先级**: P0 (必须)

**实现步骤**:
```java
private boolean isOwnedBy(ItemStack stack, Player player) {
    // 如果物品没有绑定所有者,首次使用时绑定
    if (!stack.hasTag() || !stack.getTag().hasUUID("OwnerUUID")) {
        stack.getOrCreateTag().putUUID("OwnerUUID", player.getUUID());
        return true;
    }

    UUID ownerUUID = stack.getTag().getUUID("OwnerUUID");
    return ownerUUID.equals(player.getUUID());
}
```

#### ✅ 2.1.10 覆盖 `useOn()` 方法 (防止误触)
**优先级**: P2 (可选)

```java
@Override
public InteractionResult useOn(UseOnContext context) {
    // 所有交互都在 use() 中处理,useOn() 不做特殊处理
    return InteractionResult.PASS;
}
```

---

### 2.2 注册物品

#### ✅ 2.2.1 在 `CCItems.java` 中注册
**文件**: `src/main/java/net/tigereye/chestcavity/registration/CCItems.java`

**添加代码**:
```java
public static final DeferredHolder<Item, DuochongjianyingGuItem> DUOCHONGJIANYING_GU =
    ITEMS.register(
        "duochongjianying_gu",
        () -> new DuochongjianyingGuItem()
    );
```

**位置**: 在 `ITEMS` 常量之后,其他物品注册附近

#### ✅ 2.2.2 添加本地化字符串
**文件**: `src/main/resources/assets/chestcavity/lang/zh_cn.json`

**添加内容**:
```json
{
  "item.chestcavity.duochongjianying_gu": "多重剑影蛊",
  "item.chestcavity.duochongjianying_gu.desc": "主动技能: 虚实交锋\n右键召唤/召回分身\nShift+右键打开分身界面"
}
```

#### ✅ 2.2.3 创建物品模型 (可选,阶段2可暂时使用占位纹理)
**文件**: `src/main/resources/assets/chestcavity/models/item/duochongjianying_gu.json`

**最简模型**:
```json
{
  "parent": "minecraft:item/generated",
  "textures": {
    "layer0": "chestcavity:item/duochongjianying_gu"
  }
}
```

**纹理文件**: `src/main/resources/assets/chestcavity/textures/item/duochongjianying_gu.png`
- 可以暂时复制其他蛊虫纹理作为占位

---

## 🧪 测试清单

### 单元测试 (阶段2完成后)

- [ ] **测试1: 首次使用自动绑定所有者**
  - 给玩家物品 -> 右键 -> 检查NBT中是否有 `OwnerUUID`

- [ ] **测试2: 他人无法使用绑定物品**
  - 玩家A绑定物品 -> 给玩家B -> 玩家B右键 -> 验证失败提示

- [ ] **测试3: 召唤分身**
  - 玩家右键 -> 验证分身生成 -> 检查物品NBT是否有 `CloneUUID`

- [ ] **测试4: 召回分身**
  - 召唤分身 -> 再次右键 -> 验证分身消失 -> 检查NBT是否保存 `CloneData`

- [ ] **测试5: 重新召唤后恢复数据**
  - 召唤 -> 给分身放入蛊虫 -> 召回 -> 再次召唤 -> 验证蛊虫还在

- [ ] **测试6: Shift+右键打开界面**
  - 召唤分身 -> Shift+右键 -> 验证调用 `clone.openInventoryMenu()`
  - (阶段2可以验证提示消息,阶段3验证实际GUI)

- [ ] **测试7: 跨维度场景**
  - 在主世界召唤 -> 进入下界 -> 右键 -> 验证提示"分身在不同维度"

- [ ] **测试8: 分身死亡后重新召唤**
  - 召唤分身 -> 杀死分身 -> 右键 -> 验证重新生成新分身

- [ ] **测试9: 距离限制 (打开界面)**
  - 召唤分身 -> 走远 (>10格) -> Shift+右键 -> 验证"距离太远"提示

- [ ] **测试10: 快速重复点击**
  - 快速连续右键3次 -> 验证不会生成多个分身 (防抖)

---

## 📊 验收标准

阶段2完成时,应满足:

1. ✅ 物品在 `CCItems` 中成功注册,游戏内可通过 `/give` 获得
2. ✅ 右键空气可以召唤分身 (生成 `PersistentGuCultivatorClone` 实体)
3. ✅ 再次右键可以召回分身 (实体消失,数据保存到物品NBT)
4. ✅ 重新召唤后,分身数据正确恢复 (境界、皮肤、物品栏)
5. ✅ Shift+右键调用 `clone.openInventoryMenu()` (阶段3前可以是占位提示)
6. ✅ 所有权校验生效,他人无法使用绑定物品
7. ✅ 跨维度场景正确处理 (提示或惰性清理)
8. ✅ 分身死亡后,下次召唤生成新分身
9. ✅ 无重大bug或崩溃
10. ✅ 本地化字符串正确显示

---

## 📦 交付物

### 代码文件
1. `DuochongjianyingGuItem.java` (核心物品类, ~300-400行)
2. `CCItems.java` (更新: 添加物品注册)
3. `zh_cn.json` (更新: 添加本地化字符串)

### 资源文件 (可选)
4. `duochongjianying_gu.json` (物品模型)
5. `duochongjianying_gu.png` (物品纹理,可占位)

### 文档
6. 本文档 (`阶段2_物品与交互_任务清单.md`)

---

## 🔗 依赖与接口

### 依赖阶段1的接口
- `PersistentGuCultivatorClone.spawn(ServerLevel, Player, Vec3)`
- `PersistentGuCultivatorClone.serializeToItemNBT()`
- `PersistentGuCultivatorClone.deserializeFromItemNBT(CompoundTag)`
- `PersistentGuCultivatorClone.openInventoryMenu(ServerPlayer)` (占位,阶段3实现)

### 为阶段3提供的接口
- 物品NBT结构:
  - `OwnerUUID`: UUID (所有者)
  - `CloneUUID`: UUID (当前分身实体ID,临时)
  - `CloneData`: CompoundTag (分身序列化数据,持久)
  - `DimensionKey`: String (分身所在维度)

---

## ⚠️ 注意事项

### 关键设计决策
1. **Shift优先处理**: `use()` 方法先检查Shift,再处理召唤/召回 (修正v1.0交互不一致)
2. **所有权绑定**: 首次使用自动绑定,后续检查UUID匹配
3. **惰性清理**: 不主动清理悬挂UUID,等待下次操作时自然清理
4. **专用序列化**: 使用 `serializeToItemNBT`,不复用实体生命周期方法

### 潜在风险
- ⚠️ **快速重复点击**: 可能生成多个分身 (需要防抖或冷却)
- ⚠️ **维度切换**: 分身留在原维度,需要清晰提示用户
- ⚠️ **分身死亡**: 确保 `die()` 正确清理物品状态

### 调试技巧
- 使用 `ChestCavity.LOGGER.debug()` 记录关键操作
- 在测试时用 `/data get entity @e[type=chestcavity:persistent_gu_cultivator_clone,limit=1]` 检查实体数据
- 用 `/data get entity @p SelectedItem.tag` 检查物品NBT

---

## 📅 时间线

| 子任务 | 预计工作量 | 完成标志 |
|--------|----------|---------|
| 2.1.1-2.1.10 (核心逻辑) | 2天 | `DuochongjianyingGuItem.java` 完成,所有方法实现 |
| 2.2 (注册与资源) | 0.5天 | 物品在游戏内可获得,本地化正确 |
| 测试与修复 | 0.5天 | 所有验收标准通过 |

**总计**: 3天

---

**下一阶段**: 阶段3 (界面系统 + Capabilities API)
**文档版本**: v1.0
**创建日期**: 2025-11-14
