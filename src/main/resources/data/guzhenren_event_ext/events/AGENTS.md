# Directory: data/guzhenren_event_ext/events

This directory holds the data-driven event definitions as JSON files. Each `.json` file in here should define one or more events that the system will load at startup. This allows for adding or changing events without needing to recompile the mod.
